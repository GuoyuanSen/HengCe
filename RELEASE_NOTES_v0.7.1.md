# 衡策 v0.7.1

## 启动自动分析修复

- 修复已有观察列表时启动渲染引用了不存在的 `holding` 变量并触发 `holding is not defined`，导致量化看板一直停留在骨架屏的问题。
- 默认股票代码此前已经正确恢复，但异常发生在启动自动分析调用之前，因此没有真正发出首次报价请求；手动点击“分析”后才会恢复。
- 观察列表行现在统一使用当前映射项 `item` 的代码和名称，不再受到持仓变量影响。

## 启动容错

- 把默认标的的自动行情请求提前到可选模块渲染之前。
- 持仓、观察提醒、组合风险、AI、交易复盘和交易日历的初始渲染分别隔离；单个本地模块即使遇到异常，也不会再阻断量化看板自动请求。
- 保留 v0.7.0 的报价优先、骨架屏、默认今日分时和后台补齐历史数据逻辑。

## 风险提示

本次为启动流程修复，不改变量化评分、选股条件、交易计划、回测或风险仓位规则。
