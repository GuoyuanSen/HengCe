# 衡策 v0.7.0

## 从研究到真实复盘

- 新增“交易复盘”一级菜单，把买前计划、实际成交、催化剂、信号验证和复盘统计集中在同一工作区。
- 真实交易流水支持买入、卖出、成交价、股数、实际费用、策略、交易理由、复盘结论和错误标签。
- 新增交易默认同步本地持仓；买入按实际成本和费用更新综合成本，分批卖出保留原成本并计算真实已实现盈亏。
- 旧持仓没有可验证建仓日期时不强算持有天数；删除流水也不会悄悄倒推修改持仓。
- 复盘统计按策略、行业、持有周期和错误类型展示胜率、盈亏比、已实现盈亏、费用与待复盘数量。

## 买前计划与风险闸门

- 可把量化看板的回踩区、模型失效位、估值约束和风险股数保存为本机交易计划。
- 计划明确入场区、止损、目标、参考股数、触发条件、交易逻辑和有效期。
- 执行前逐项检查价格结构、盈亏比、量化评分、行情时效、市场档位、风险预算、近期事件、组合集中度和计划是否过期。
- 检查结果只分为“条件完整、需要确认、暂不执行”，不会连接券商或自动下单。
- 量化看板顶部在存在临近事件或组合风险时显示“今日需要关注”，可直接进入交易复盘核对。

## 催化剂日历

- 按当前股票、持仓和观察列表读取公开财报预约、限售解禁、分红除权及股东增减持。
- 显示事件日期、股票、事件类型、披露来源和风险说明；部分来源失败时保留其余可用事件。
- 支持在本机手工记录宏观会议、经营事项或本人关注日期。
- 预约日期可能变化，解禁不等于实际减持，除权也不等于基本面改变；页面持续保留这些边界提示。

## 真实信号前向验证

- A股优选每日前列标的、尾盘实际入选标的和已保存交易计划会冻结发生时的价格、评分与风险线。
- 统一以下一交易日开盘作为验证起点，跟踪 1、3、5、10 个交易日净收益和指数超额。
- 验证计入佣金、印花税和滑点，并保留最大顺向与逆向波动；周期尚未发生时保持“跟踪中”。
- 修复上海市场超额基准可能被误识别为同代码股票的问题，改用受限的专用沪深指数日线接口。

## 日韩市场与全球半导体链

- 市场风向标新增日经225、韩国 KOSPI 和台湾加权指数。
- 新增全球半导体链观察区，交叉展示费城半导体指数、三星电子、SK海力士、Tokyo Electron、爱德万测试和台积电 ADR。
- 韩国存储、日本设备、台湾代工和美国风险偏好只作为 A 股半导体外部确认，不直接写入个股买卖结论。
- 海外补充行情采用单次批量请求；单一市场缺失不会使整个风向标不可用。

## 量化看板首屏体验

- 首次进入先显示结构化骨架屏，不再只显示空白区域和持续旋转图标。
- 最新报价与历史量价计算拆分：报价返回后立即展示价格和日内数据，约180根日K与评分随后补齐。
- 有本地快照时先显示并标注后台刷新；没有快照时分阶段说明当前正在读取的内容。
- 版本检查和交易日历同步延后到首屏关键请求之后，避免启动时争抢网络。
- 启动失败不再依次对多个候选股票进行长时间联网重试，只在存在可立即展示的本地快照时切换备用标的。
- 每次新启动默认显示“今日分时”；同一运行期间切换菜单再返回，仍保留用户刚刚选择的图表模式。

## 更新检查可靠性

- 更新检查、行情和 AI 错误使用独立文案，GitHub 返回 403 不再显示为“行情服务返回 403”。
- GitHub Release API 限流或超时时自动尝试官方 latest 重定向，并使用可预测且经过 SHA-256 校验的安装包名称。
- 两种联网方式都失败时显示最近一次成功检查结果并明确标注未实时确认，不把缓存伪装成最新结果。
- 更新失败或当前架构不支持应用内下载时提供“打开下载页”。

## 本地备份与恢复

- 参数设置新增本地备份与恢复，覆盖持仓、观察列表、交易流水、计划、复盘、手工事件、信号验证和个人设置。
- 可选备份密码；设置后通过 scrypt 派生密钥并使用 AES-256-GCM 加密。
- 恢复前需要本人确认，导入内容经过键名允许列表和 JSON 校验。
- AI API Key、行情缓存、文件路径和应用外数据不会进入备份。

## 导航与阅读体验

- 菜单调整为“量化看板 → 每日热点 → A股优选 → 市场情报 → 市场风向标 → AI追踪 → 尾盘观察 → 观察提醒 → 交易复盘 → 策略回测 → 持仓管理 → 参数设置”。
- A股优选紧随每日热点，符合“先看方向、再看股票”的使用顺序；AI追踪移动到市场风向标之后。
- 公司画像摘要字号小幅增大，完整简介使用更大字号、较宽行高、分段和独立阅读背景。
- 主营业务降低过重字重，事实标签与深浅主题同步提高可读性。

## 风险提示

真实流水和计划只反映用户手工输入，软件无法确认券商实际成交是否一致。公开行情、披露日历和海外市场数据可能延迟、缺失或调整；所有计划、闸门、事件映射、AI 解读和前向统计均为研究辅助，不构成投资建议，也不会自动交易。
