# 衡策 v0.4.1

本次版本修复桌面端更新替换与 Windows 后台运行体验。

## macOS 更新

- 新版 DMG 下载并通过 SHA-256 校验后，安装按钮明确显示“打开 DMG 并退出”。
- 用户点击后先打开 DMG，再延迟退出当前衡策，避免运行中的应用无法被新版替换。
- 持仓、观察列表和参数仍保存在用户数据目录，替换应用本体不会清空本地数据。

## Windows 系统托盘

- Windows 运行时创建衡策系统托盘图标。
- 默认点击最小化后隐藏到通知区域，尾盘扫描和观察提醒继续运行。
- 首次最小化会提示衡策仍在后台运行。
- 单击或双击托盘图标可恢复窗口。
- 托盘右键菜单提供打开、隐藏、切换“最小化到系统托盘”和退出功能。
- 参数设置中可以关闭“最小化到系统托盘”；关闭窗口仍然正常退出软件。

衡策仍不连接券商账户，也不会自动下单。
