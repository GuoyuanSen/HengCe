# 衡策 v0.3.3

这一版新增应用内更新流程。普通用户无需自行前往 GitHub 判断和下载系统对应的安装包。

## 应用内更新

- 应用启动后自动检查 GitHub Release 的最新正式版本。
- 参数设置页新增“检查更新”，展示当前版本、最新版本、安装包大小和更新说明。
- 自动选择 Apple Silicon DMG 或 Windows x64 安装程序，不下载其他平台产物。
- 下载过程展示实时进度，完成后强制核对 Release 中的同名 SHA-256 文件。
- 校验失败的安装包会立即删除，不能从应用内打开。
- Windows 校验成功后可直接启动安装程序；macOS 会打开 DMG，由用户确认替换应用。

## 安全边界

- 更新检查、下载、校验和安装包打开全部在 Electron 主进程完成。
- 渲染页面不能指定任意下载地址或本地文件路径。
- 只接受仓库最新的非草稿、非预发布 GitHub Release。
- 当前 macOS 公开包使用临时签名，因此不进行无提示的后台自替换。
