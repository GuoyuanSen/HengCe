# 衡策 v0.5.0

## 新增 AI 追踪

- 新增“AI追踪”一级菜单，统一追踪当前标的、持仓和观察列表。
- 每次分析先读取最新行情、日 K、估值和市场环境，再生成结构化事实包。
- 自动保存本机追踪快照，突出量化评分、价格与风险结构相对上次的变化。
- 展示积极因素、主要风险、回踩观察区、突破确认位、策略失效位与下次检查项。
- 未配置 API Key 或 AI 服务失败时，仍会生成本地量化摘要，不影响原有行情和回测能力。

## 公司画像与尾盘市场范围

- 量化看板新增醒目的所属行业标签和“公司画像”，展示板块路径、主营业务、公司简介及基础公司信息。
- 公司与业务资料同时进入 AI 事实包，帮助模型在量价结构之外理解标的业务背景。
- 尾盘观察默认仅筛选沪深主板，自动排除创业板与科创板。
- 支持切换为“主板 + 创业板”“主板 + 科创板”或“全部可用 A 股”，选择保存在本机并进入筛选漏斗。
- 修复部分股票分时图长期停留在“等待数据”的问题：新增腾讯分时主源和东方财富多节点降级，并显示实际数据来源。

## AI 配置与隐私

- 参数设置新增 Responses API 地址、模型、API Key、连接测试和删除 Key。
- API Key 只在 Electron 主进程使用，优先调用 macOS/Windows 系统加密能力保存，不进入页面存储、日志或源码。
- 默认不向 AI 发送持仓股数和成本；用户可在参数设置中单独授权。
- AI 输出使用固定 JSON Schema，并明确限制为解释已有公开事实，不允许虚构新闻、公告或精确价格。

## 风险提示

AI 追踪属于研究辅助，不构成投资建议，不连接券商，也不会自动下单。行情接口与模型服务都可能延迟、中断或产生错误，实际决策应结合公告原文、风险承受能力和独立判断。
